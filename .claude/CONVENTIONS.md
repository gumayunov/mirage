## Git Workflow
- NEVER use `git worktree`. Always work in the current working directory.
- Create a feature branch from current branch if needed, but stay in the same working copy.
- Do not create separate working trees for implementation.
